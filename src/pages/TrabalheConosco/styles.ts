import styled from 'styled-components';

export const Wrapper = styled.div`
    height: 100vh;
    width: 100%;
    display: flex;
    justify-contente: space-between;
    flex-direction: row;
    background: #282828;
`;
