import styled from 'styled-components';

export const Wrapper = styled.div`
    height: 10rem;
    display: flex;
    align-items: center;
    justify-content: center;

    > p {
        font-size: 1.4rem;
        color: black;
    }
`;
